<div class="container p-4">
  <div class="row d-flex justify-content-center align-items-center">
    <% if current_user %>
      <%= render partial: 'home/partials/authenticated', locals: { user: @user } %>
    <% else %>
      <%= render partial: 'home/partials/no_authenticated' %>
    <% end %>
  </div>
</div>
