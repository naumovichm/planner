<%= form_with model: category do |f| %>
  <%= render partial: 'layouts/error', locals: { model: category, attribute: :name } %>
  <div class="mb-4">
    <%= f.text_field :name, class: "form-control name-cat", placeholder: t('category.name') %>
  </div>
  <p><%= f.submit t('btn.create'), class: "btn btn-primary" %></p>
<% end %>
