<div class="container p-4">
  <div class="row d-flex justify-content-center align-items-center">
    <div class="card col-4 p-4 unlock">
      <div class="card-body p-4 text-center">
        <h2><%= t('devise.unlock_resend') %></h2>
        <%= form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post }) do |f| %>
          <%= render partial: "users/shared/error_messages", locals: { user: resource, attribute: :email } %>
          <div class="field">
            <%= f.email_field :email, class:'form-control', autofocus: true, 
                autocomplete: "email", placeholder: t('user.email') %>
          </div>
          <div class="actions">
            <%= f.submit t('resend_btn'), class: "btn btn-primary" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
